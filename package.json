{
  "name": "rendezvue",
  "version": "0.4.0-alpha.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 4173 --directory apps/web",
    "dev:private-preview": "python3 -m http.server 4174 --directory dist-private-preview",
    "build:static": "node scripts/build-static.mjs",
    "build:hf": "npm run build:static && node scripts/build-hf-deploy.mjs",
    "build:private-preview": "node scripts/build-private-preview.mjs",
    "validate:private-preview": "node scripts/validate-private-preview.mjs",
    "check:private-preview": "node --check apps/private-preview/app.js && node --check scripts/build-private-preview.mjs && node --check scripts/validate-private-preview.mjs && npm run build:private-preview && npm run validate:private-preview",
    "test": "node --test apps/web/tests/*.test.mjs",
    "validate": "node scripts/validate-static.mjs",
    "check": "npm run build:hf && npm run validate && npm test"
  },
  "engines": { "node": ">=20" }
}
