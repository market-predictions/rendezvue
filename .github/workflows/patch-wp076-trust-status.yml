name: Patch WP076 trust status

on:
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/patch-wp076-trust-status.yml'
      - 'scripts/patch-wp076-trust-status.py'

permissions:
  contents: write

jobs:
  patch:
    if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
    runs-on: ubuntu-latest
    env:
      SUPABASE_URL: https://example.supabase.co
      SUPABASE_PUBLISHABLE_KEY: sb_publishable_ci_only_placeholder_00000000000000000000
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0
      - name: Apply trust-status refinement and test
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'rendezvue-implementation-bot'
          git config user.email 'rendezvue-implementation-bot@users.noreply.github.com'
          python scripts/patch-wp076-trust-status.py
          npm ci
          node --test apps/web/tests/profile-media-architecture.test.mjs
          npm run build:cloudflare
          node scripts/validate-wp076-profile-media.mjs
          git add apps/private-preview/profile-media-controller.js apps/private-preview/profile-media.css apps/web/tests/profile-media-architecture.test.mjs
          git commit -m 'Make WP076 live-selfie trust status truthful'
          git push origin HEAD:${{ github.event.pull_request.head.ref }}
